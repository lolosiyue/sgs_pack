游戏版本尽量升级到最新版（20220402及以上），因为部分代码旧版没有。（可能导致无法运行）

4.0及以上增加了自己定义的AI函数，与神杀主体包兼容但是不能相互覆盖，在进行主体更新后，可能会因AI数据覆盖而失去自定义的AI函数，会导致《极乐包》AI失效，也有可能会导致游戏无法运行，解决方法是重新再次覆盖《极乐包》，这样就会将自定义的AI函数恢复。
（；´д｀）ゞ

AI函数的使用说明在极乐包AI文件里：QSanguosha/lua/ai/jilebao-ai.lua

我简单修改了一下AI的出牌，可以使AI在牌较多时（约三四十张时）也能较快出牌；这个修改只要神杀装有《极乐包》就有，不需要勾选开启《极乐包》。

6.0增加了妖周泰，所以增加了妖势力，但是为防止神势力开局选择妖势力，所以暂时隐藏了妖势力，这造成了不能正常选择妖周泰出场；如果想解除限制，可以到 QSanguosha\extensions\jilebao.lua 里的第8行，将 kingdom_yao = false 改为 kingdom_yao = true ，就会解除妖势力限制。

同时6.0也增加了直接解除《极乐包》隐藏武将的功能，可以到 QSanguosha\extensions\jilebao.lua 里的第9行，将 hidden = true 改为 hidden = false ，就会解除武将的隐藏，这样AI就会有可能选择这些隐藏的武将。

6.0还增加了一个有趣的模式【缤芬模式】；开始游戏时会提示你是否发动。
【缤芬模式】：AI 有几率会犯病，让场上局势更佳“多姿多彩”。

7.0可以手动关闭【缤芬模式】了，不想在游戏开始时有询问，可以关闭；到 QSanguosha\extensions\jilebao.lua 里的第10行，将 jl_bingfen = true 改为 jl_bingfen = false ，就会不再询问是否开启。

8.0可能会和神杀主体0517版代码冲突，如果你的版本为0517及以上，就先不要装上8.0了。

8.1经过修改是适合神杀主体0517版的，所以版本为0517的就可以装上8.1；但是8.1就不适合0517之前的版本了。（8.0和8.1内容一样，只是版本适应不同）

9.0修改缤芬模式只有在《极乐包》启用时才会有提示；想要不勾选《极乐包》也可以启用缤芬模式，就需要到 QSanguosha\extensions\jilebao.lua 里的第10行，将 jl_bingfen = true 改为 jl_bingfen = 1 ，就会所有模式都提示；如果想不提示就启用缤芬模式，可以将jl_bingfen = true 改成 jl_bingfen = 2 ，则将全模式直接启用缤芬模式，不再有询问提示。

9.1修改缤芬模式的启用，在卡牌包那里选择是否启用缤芬模式，默认关闭


--Lua学生--